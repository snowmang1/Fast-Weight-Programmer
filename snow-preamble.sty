\ProvidesPackage{snow-preamble}

\title{ Fast weight programmers implementation}
\author{
	Evan M Drake \\
	\texttt{}
}

\newcommand{\myquote}[1]{``#1''}
\newcommand{\works}[1]{\textbf{#1\ \ }}
\usepackage[backend=biber]{biblatex}
\addbibresource{bib/refs.bib}

\usepackage{listings, xcolor, pgfplots, amsmath, amssymb, graphicx, hyperref, tikz, placeins, imakeidx,
	empheq, placeins, rotating}
\usetikzlibrary{quotes, mindmap, patterns}
\usetikzlibrary{shapes.geometric, arrows}

\makeindex[columns=2, title=Index, options= -s indexstyle.ist, intoc]

\graphicspath{{./img/}}

\pgfplotsset{width=14cm, compat=1.18}

\lstdefinestyle{code}{
	numbers=left,
	stepnumber=2,
	breaklines=true,
	postbreak=\mbox{\textcolor{red}{$\hookrightarrow$}\space},
	numberstyle=\tiny,
	basicstyle=\footnotesize,
	numberblanklines=false,
	keywordstyle=\color{blue},
	commentstyle=\color{gray},
	identifierstyle=\color{black},
	frame=tbrl,
	frameround=tttt,
	backgroundcolor=\color{black!3!red!3}
	}

\lstdefinestyle{pseudocode}{
	language=C,
	keywordstyle=\color{blue},
	commentstyle=\color{gray},
	identifierstyle=\color{black},
	frame=shadowbox,
	rulesepcolor=\color{blue!50!green},
	frameround=ffft,
	}

\def\backmatter{
	\pagenumbering{Roman}
	\setcounter{page}{1}
	\setcounter{section}{0}
	}

\newcommand{\In}{\indent\indent}
\newcommand{\reff}[1]{$^{\ref{#1}}$}
% command will pull body files from my body dir
\newcommand{\mybody}[1]{\input{texbody/#1.latex}}

\date{22/10/2023}
