name: Haskell CI

# FIX:
# need to only test inside the valid code/fwp directory

on:
  push:
    branches: ["THE-61"]
  pull_request:
    branches: ["trunk"]

jobs:
  setup:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        with:
          path: 'code/fwp'
      - name: Stack setup
        run: stack upgrade
      - name: Test
        run: stack test
